---

pkgs:
  - firewalld

firewalld_opts:
  default_zone: public
  zones:
    - name: public
      target: DROP
      allowed_services:
        - ssh
    - name: drop
      allowed_services:
        - ssh

